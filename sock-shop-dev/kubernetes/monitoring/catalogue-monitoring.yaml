apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: catalogue-metrics
  namespace: sock-shop
  labels:
    release: prometheus      
spec:
  selector:
    matchLabels:
      app: catalogue
  namespaceSelector:
    matchNames:
      - sock-shop
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
